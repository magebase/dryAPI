"use client"

/**
 * HeroGradientCanvas formerly used WebGL (gradient-gl) for a dynamic background.
 * It is currently disabled to reduce computation cost and eliminate lag.
 * The hero section now relies on the layered CSS gradients in home-sections.tsx.
 */
export function HeroGradientCanvas() {
  return null
}
